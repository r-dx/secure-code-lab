# Contributing

Keep examples minimal, local, and non-weaponized. Every unsafe behavior and corrected control must have a regression test. Do not add live targets, secrets, destructive payloads, or unsupported security claims. Run `python -m pytest -q` before proposing a change.
